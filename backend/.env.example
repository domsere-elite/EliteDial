PORT=5000
NODE_ENV=development

# Database (Supabase Postgres)
DATABASE_URL="postgresql://postgres.<project-ref>:<password>@aws-1-us-east-2.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.<project-ref>:<password>@aws-1-us-east-2.pooler.supabase.com:5432/postgres"

# ─── Supabase Auth ─────────────────────────────────
# Copy from Supabase dashboard → Project Settings → API.
# JWT verification uses the project's published JWKS (asymmetric ES256), so
# no shared HS256 secret is required here.
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=sb_publishable_...        # also accepts legacy "eyJhbGci..." anon key
# DO NOT commit. DO NOT expose to frontend. Server-side only.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...     # or new "sb_secret_..." format

# SignalWire
SIGNALWIRE_PROJECT_ID=
SIGNALWIRE_API_TOKEN=
SIGNALWIRE_SPACE_URL=

# Retell AI (needed for AI_AUTONOMOUS mode)
RETELL_API_KEY=
RETELL_API_BASE_URL=https://api.retellai.com
RETELL_AGENT_ID=
RETELL_WEBHOOK_SECRET=
RETELL_DEFAULT_FROM_NUMBER=
RETELL_FALLBACK_NUMBER=

# URLs
FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:5000
NEXT_PUBLIC_APP_NAME=EliteDial
BACKEND_PUBLIC_URL=http://localhost:5000

# CRM (optional)
CRM_BASE_URL=
CRM_API_KEY=
CRM_WEBHOOK_URL=
CRM_WEBHOOK_SECRET=

# Telephony defaults
DEFAULT_OUTBOUND_NUMBER=
RECORDING_ARCHIVE_BASE_URL=
TRANSCRIPT_ARCHIVE_BASE_URL=

# Dialer worker
DIALER_MODE=mock
DIALER_POLL_INTERVAL_MS=5000
